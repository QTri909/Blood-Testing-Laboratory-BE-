spring:
  profiles:
    active: dev
  #  devtools:
  #    add-properties:
  restart:
    enabled: false
  livereload:
    enabled: false
  application:
    name: warehouse-service
  datasource:

#      url: jdbc:postgresql://localhost:5432/warehouse
#      username: postgres
#      password: 123456
#      driver-class-name: org.postgresql.Driver


    ## Khanh's Supabase PostgreSQL
    # url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require
    # username: postgres.utosgzbzcjenqzpruter
    # password: 123456
    # driver-class-name: org.postgresql.Driver

    ## Cuong's Aiven PostgreSQL
    url: jdbc:postgresql://darkkernel-postgresql-333-darkkernel-aiven.j.aivencloud.com:11868/defaultdb?sslmode=require
    username: avnadmin
    password: AVNS_ZPFNWkDP1CZwYC0bULB
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 60000
      connection-timeout: 20000
      validation-timeout: 5000
      connection-test-query: SELECT 1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: warehouse-service-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "*"
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

      retries: 3
      properties:
        spring.json.add.type.headers: false
aws:
  region: ap-southeast-2
  access-key-id: AKIAUAT3BD6HAAX4OYWX
  secret-key-secret: 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

audit:
  cloudwatch:
    log-group: warehouse-service
    enabled: true

grpc:
  server:
    port: 9002
    enable-keep-alive: true
    keep-alive-time: 30s
    keep-alive-timeout: 5s
    max-concurrent-streams: 100
  client:
    test-order-service:
      address: static://localhost:9001
      negotiationType: plaintext
      enableKeepAlive: true
      keepAliveTime: 20s
      keepAliveTimeout: 5s

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    prometheus:
      enabled: true

server:
  port: 8086

