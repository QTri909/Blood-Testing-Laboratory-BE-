version: '3.8'
services:
  iam-service:
    build:
      context: ..                      # 👈 cho phép Docker thấy parent POM
      dockerfile: iam-service/Dockerfile
    container_name: iam-service
    image: iam-service:latest
    ports:
      - "8081:8081"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://iam-db:5432/postgres
      - SPRING_DATASOURCE_USERNAME=yoko
      - SPRING_DATASOURCE_PASSWORD=hoang229
      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:29092
      - SPRING_KAFKA_CONSUMER_GROUP_ID=iam-service-group
      - AWS_ACCESS_KEY_ID=AKIAUAT3BD6HNMSHTVF2
      - AWS_SECRET_ACCESS_KEY=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
      - AWS_REGION=ap-southeast-2
    depends_on:
      - kafka
      - iam-db
    networks:
      - app-network

  kafka:
    image: confluentinc/cp-kafka:latest
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@kafka:29093'
      KAFKA_LISTENERS: 'PLAINTEXT://kafka:29092,CONTROLLER://kafka:29093'
      KAFKA_ADVERTISED_LISTENERS: 'PLAINTEXT://kafka:29092'
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: 'PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT'
      KAFKA_INTER_BROKER_LISTENER_NAME: 'PLAINTEXT'
      KAFKA_CONTROLLER_LISTENER_NAMES: 'CONTROLLER'
      CLUSTER_ID: 'MkQkDjFzNTcwNTJENDM2Qk'
    networks:
      - app-network

  iam-db:
    image: postgres:16
    container_name: iam-db
    environment:
      - POSTGRES_USER=yoko
      - POSTGRES_PASSWORD=hoang229
      - POSTGRES_DB=postgres
    ports:
      - "5432:5432"
    volumes:
      - iam-data:/var/lib/postgresql/data
    networks:
      - app-network

volumes:
  iam-data:

networks:
  app-network:
    driver: bridge

